namespace Cobalt.Tui.Tests;

public class PlaceholderTests
{
    // Keeps the runner from reporting "no tests" until M2 lands real view-model tests.
    [Fact]
    public void TestInfrastructure_Runs() => Assert.True(true);
}
